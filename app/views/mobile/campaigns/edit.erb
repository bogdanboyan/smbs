<%= render :partial=> 'builder' %>

<script>
  var builder = new Builder(<%= raw render_partials_to_js_object %>);
  var page_id = <%= @mbc.id %>;
  $(document).ready(function() {
    switch_save_to_update();
    builder.inject_serialized_document(<%= raw @mbc.document_model %>);
  });
</script>