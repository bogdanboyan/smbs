<script>
$(document).ready(function() {
  SMBS.MobileCampaign.ImageGalleryWidget.init('<%= @campaign.try(:id) %>')
  
<% if @campaign %>
  SMBS.MobileCampaign.ImageGalleryWidget.enable()
<%else%>
  SMBS.MobileCampaign.ImageGalleryWidget.disable()
<%end%>
})
</script>

<h2>Галлерея</h2>

<div class="assets">
  <%= render :partial => 'image_asset', :collection => @images %>
  <span class="notice">Вы не можете загружать изображения в документ который не сохранен</span>
</div>

<div class="clear">&nbsp;</div>

<div id="file-uploader"></div>

