<%= render :partial=> 'builder' %>

<script>
  var builder = new Builder(<%==  render_partials_to_js_object %>);
  var page_id = <%= @campaign.id %>;
  $(document).ready(function() {
    switch_save_to_update();
    builder.inject_serialized_document(<%== @campaign.document_model %>);
  });
</script>
