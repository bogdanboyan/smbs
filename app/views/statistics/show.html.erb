<% content_for :head do %>
   <script type="text/javascript" src="http://www.google.com/jsapi"></script>
   <%= javascript_include_tag 'analytic/shortener' %>
<%end%>


<script>
  SMBS.Shortener.Clicks.init(<%= @short_url.id %>);
  SMBS.Shortener.Regions.init(<%= @short_url.id %>);
</script>

<div class="grid_8 alpha">
  <h2>Cтатистика переходов<%= ": " + truncate(@short_url.mobile_campaign.title) if @short_url.mobile_campaign %></h2>
  <p>Ресурс: <%= short_url_title_with_origin_link_pair(@short_url) %></p>
  <p>Общее количество просмотров страницы (за весь период): <strong><%= @short_url.clicks_count %></strong></p>
  <div id="clicks_report"></div>

  <h2>Статистика по городам</h2>
  <div id="regions_report"></div>
  
</div>

<div class="grid_4 omega">
    <p><%= link_to "Статистика в таблице", details_statistic_path(@short_url), :class => "button" %></p>
    <p><strong>КАК ИСПОЛЬЗОВАТЬ:</strong></p>
</div>